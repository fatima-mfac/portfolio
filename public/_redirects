# Cloudflare Pages redirects.
# (Next's redirects() can't run in a static export — see next.config.ts.)

# Old dev route names → clean public case-study URLs.
/patina-v3     /patina    301
/vodafone-v3   /vodafone  301
/herc-v3       /herc      301

# Herc's project slug differs from its URL — send the slug form to the page.
/herc-rentals  /herc      301

# Zebra Finch has no standalone page — open it as the homepage overlay.
/zebra-finch   /?project=zebra-finch   301
